public interface RaceRegister {

    // Whoever is doing this one can I, Daniel, request that the register method returns a string saying successful or failed
    // Please also have the register method have the signature public String register(int category, Race race, Racer racer)
}
